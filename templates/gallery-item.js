import React from "react";
import { graphql } from "gatsby";

const GalleryItem = ({ data }) => {
    <div>
        <h1>Gallery Page</h1>
        <p>View images here</p>
    </div>
}

export default GalleryItem;